# ============================================
# SmartFlow 跟单机器人 - 环境变量模板
# 使用方式：复制为 .env 后填入真实值
# cp .env.example .env
# ============================================

# --- 必填：API 与钱包 ---
# Helius RPC/WS/API 密钥 (https://www.helius.dev/)
HELIUS_API_KEY=your_helius_api_key_here

# 跟单目标钱包地址（要跟随的「聪明钱」Solana 地址）
TARGET_WALLET=your_target_smart_money_wallet_address

# 交易钱包私钥（Base58，用于执行跟单买入/卖出的钱包）
PRIVATE_KEY=your_base58_private_key_here

# --- 可选：机器人标识 ---
# 用于邮件标题等展示，默认 NONAME
BOT_NAME=SmartFlow

# --- 策略（本币 USDC）---
# 每次跟单买入金额（USDC），默认 10
COPY_AMOUNT_USDC=10
# 单币最大持仓成本（USDC），超过则不再加仓该币，默认 200
MAX_POSITION_USDC=200

# 买入滑点 (bps)，默认 1000 = 10%
SLIPPAGE_BUY=1000
# 卖出滑点 (bps)，默认 2000 = 20%
SLIPPAGE_SELL=2000

# 止盈倍数：收益率达到该倍数时触发「留种」止盈，默认 10 (即 1000%)
TAKE_PROFIT_ROI=10
# 止盈时卖出比例 (0~1)，默认 0.5 = 50%
TAKE_PROFIT_SELL_PCT=0.5
# 止损比例 (0~1)，亏损达到该比例时全仓止损，默认 0.5 = 50%
STOP_LOSS_PCT=0.5

# --- 风控 ---
# 池子最小流动性 (USD)，低于不跟，默认 3000
MIN_LIQUIDITY_USD=3000
# 市值上限 (FDV USD)，超过不跟，默认 5000000
MAX_FDV=5000000
# 市值下限 (FDV USD)，默认 0
MIN_FDV=0
# 大哥单笔买入最小金额 (SOL)，用于过滤试盘，默认 1
MIN_SMART_MONEY_COST=1
# 单币最大买入次数硬限制，默认 20
MAX_BUY_COUNTS_HARD_LIMIT=20

# --- 邮件（可选，不填则不发邮件）---
EMAIL_SENDER=your_qq_email@qq.com
EMAIL_PASSWORD=your_qq_smtp_authorization_code
EMAIL_RECEIVER=receiver@example.com

# --- Jupiter API（可选，减少限流）---
# https://station.jup.ag/docs/apis/api-key
JUPITER_API_KEY=

# --- 日报发送时间（本地时间）---
# 格式 HH:MM，默认 09:00
DAILY_REPORT_TIME=09:00

# --- Jito MEV ---
# 是否使用 Jito 发送交易，默认 true
USE_JITO=true
# Jito 小费 (SOL)，默认 0.0001
JITO_TIP_AMOUNT=0.0001
# Jito Block Engine 地址，美西可用 SLC 节点
JITO_BLOCK_ENGINE_URL=https://slc.mainnet.block-engine.jito.wtf/api/v1/bundles
